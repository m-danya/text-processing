Использовал TfidfVectorizer и KNN. Нормалайзер в итоге
не стал использовать. Взял n_neighbors=1, т.к. датасет маленький.
После каждого предикта добавляю текст и предикшн внутрь модели, чтобы
новые ответы базировались в т.ч. на новых (пусть и только что
предсказанных) данных.

Когда всё уже сделал и побил бейзлайн, понял, что трейн часть
можно было спокойно запихнуть в `__init__` солюшна и обойтись
без pickle вообще.

Как воспроизвести:
1. Запустить train.py
2. Создадутся (или перезапишутся) файлы `model.pkl` и `tokenizer`.pkl
3. Всё